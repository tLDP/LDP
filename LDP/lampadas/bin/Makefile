binprefix = /usr/bin

INSTALLDIR = install -d
INSTALLBIN = install

all:	build

build:	

install: all
	$(INSTALLBIN) lampadasweb  $(binprefix)/lampadasweb
	$(INSTALLBIN) lampadasdb  $(binprefix)/lampadasdb

clean:

