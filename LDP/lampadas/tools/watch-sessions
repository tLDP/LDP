#/bin/bash
# 
# This script displays a running list of sessions, sorted by age.
# 
watch -n 1 "psql scratch -c \"select username, timestamp, age(timestamp, now()), abs(extract(minute from age(timestamp, now()))) from session order by timestamp\""
